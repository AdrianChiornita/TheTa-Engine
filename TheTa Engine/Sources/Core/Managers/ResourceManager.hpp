#pragma once

#include <string>

// add rersources for the engine

namespace rsc
{
	const std::string root = "Resources/";
	const std::string models = root + "Models/";
	const std::string textures = root + "Textures/";
	const std::string shaders = root + "Shaders/";
}